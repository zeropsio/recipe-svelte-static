# You don't need this in your codebase; zerops.yml is used for deployment.

project:
  name: recipe-svelte
  tags:
    - zerops-recipe

services:
  - hostname: app
    type: static
    enableSubdomainAccess: true
    buildFromGit: https://github.com/zeropsio/recipe-svelte-static